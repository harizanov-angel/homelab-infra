# Kubernetes infra setup

This repo helps us setup the infrastructure for Kubernetes clusters. It doesn't handle the clusters themselves, only the underlying infrastructure. Each forlder contains different approach.